import 'package:flutter/material.dart';

class SearchFragmentWidget extends StatelessWidget {
  final Color color;

  SearchFragmentWidget(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
